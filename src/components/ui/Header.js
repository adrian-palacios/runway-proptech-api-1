import React from "react";
import logo from "../../images/logo.png";

const header = () => {
	return (
		<header className="center">
			<img src={logo} alt="" />
		</header>
	);
};

export default header;
